# 分享给别人玩（免安装版）

游戏可以打包成一个**独立的 exe 文件**，别人不用装 Python、不用装 pygame，
双击就能玩。

---

## 怎么打包

双击 `build_exe.bat` 就行，它会自动完成四步：

1. 用 `make_icon.py` 生成 exe 图标（拿结算表情图做的）；
2. 调用 PyInstaller 打包；
3. **用打好的 exe 跑一次自检**，确认表情图和字体都被正确带进去了；
4. 打包成 `yet-arrow-away_免安装版.zip`。

产物：

| 文件 | 说明 |
| --- | --- |
| `dist/yet-arrow-away.exe` | 单文件游戏本体，约 14 MB |
| `dist/怎么玩.txt` | 给玩家的说明 |
| `yet-arrow-away_免安装版.zip` | 上面两个打成一个包，约 14 MB，直接发人 |

也可以手动执行：

```bash
python make_icon.py
python -m PyInstaller --onefile --windowed --name yet-arrow-away ^
    --icon game_icon.ico --add-data "resources;resources" ^
    --clean --noconfirm --exclude-module numpy --exclude-module mkl ^
    ... game.py
```

---

## 打包时踩的几个坑

### 1. 体积从 176MB 降到 14MB

第一次打包出来是 **176MB**，微信都发不出去（限制 100MB）。

原因：Anaconda 环境里装了 numpy，而 numpy 又依赖 Intel 的数学库 MKL。
PyInstaller 会把它们整个拖进包里，光 MKL 的 DLL 就有几十 MB。
但本项目的图形全是 pygame 画的，**一个 numpy 都不需要**。

解决办法是加 `--exclude-module` 排除掉用不到的大块头：

```
numpy  mkl  mkl_fft  mkl_random  psutil  setuptools  pkg_resources
unittest  pdb  doctest  asyncio  curses  yaml  wheel  tomli
threadpoolctl  test  lib2to3  distutils
```

排完立刻从 176MB 掉到 14.3MB。

> 怎么知道该排哪些？打包后会生成 `build/<名字>/Analysis-00.toc`，
> 里面列了所有被打进去的模块，翻一遍就能看出哪些是明显用不上的。

### 2. 资源路径：源码能跑，打包后找不到图

直接跑源码时，`__file__` 就是 `game.py` 所在目录，表情图就在旁边的 `resources/`。
但打成单文件 exe 后，程序会先把内容解压到一个**临时目录**再运行，
`__file__` 指向的是那个临时目录。

解法是统一走一个 `app_path()` 函数：

```python
def app_path(*parts):
    if getattr(sys, "frozen", False):
        base = sys._MEIPASS              # 打包后：临时解压目录
    else:
        base = os.path.dirname(os.path.abspath(__file__))
    return os.path.join(base, *parts)
```

同时打包命令里要用 `--add-data "resources;resources"` 把资源捎上
（Windows 上用分号分隔，Linux/macOS 用冒号，写错了资源就进不去）。

### 3. 中文字体不能写死一个

原来代码里写死了 `C:\Windows\Fonts\msyh.ttc`（微软雅黑）。
自己的电脑上没问题，换台电脑如果没这个字体，中文就全变成方块。

改成按候选列表依次尝试，微软雅黑 → 黑体 → 宋体 → 等线，
全都没有才退回 pygame 默认字体。

### 4. 打包后闪退，什么都看不到

`--windowed` 打包出来是没有控制台的，一出异常就是「双击 → 闪一下 → 没了」，
对方完全没法告诉你哪错了。

所以在 `main()` 上包了一层 try/except，出错就把堆栈写进 exe 旁边的
`错误日志.txt`，让对方把这个文件发回来即可。

### 5. 自检模式（`--smoke`）

资源路径、中文字体这类问题，**在开发者自己的机器上根本暴露不出来** ——
因为源码目录下一切正常。必须真的跑 exe 才知道。

所以游戏支持一个隐藏的自检参数：

```bash
dist\yet-arrow-away.exe --smoke 输出目录
```

它用虚拟显示驱动（`SDL_VIDEODRIVER=dummy`）不弹窗地把各个界面渲染成 PNG，
并检查表情图有没有加载到、中文字体渲染宽度是否正常，
结果写进 `自检结果.txt`。

打包脚本的第 3 步会自动跑这个自检 —— 这样每次打完包，
都能立刻知道「这个 exe 拿到别人机器上能不能正常显示」。

> 唯一测不了的：**别人的电脑没有装 Visual C++ 运行库**。
> 这个没法在自己机器上验证，只能等对方反馈（出问题的话装一次
> VC++ Redistributable 2015-2022 即可，很多电脑本来就有）。

---

## 其他分发方式

### 方式一：直接发 zip（推荐）

把 `yet-arrow-away_免安装版.zip` 通过微信 / QQ / 网盘发过去，
对方解压后双击 `yet-arrow-away.exe`。

**优点**：对方零配置。
**注意**：微信可能会提示「此文件可能不安全」，让对方选「仍然继续」；
个别杀毒软件对 PyInstaller 打的包会误报，加个信任即可。

### 方式二：直接从 GitHub 仓库下载

把 exe 传到 GitHub Release 里，让对方从 Release 页下载。
好处是自带「这是源码项目产物」的可信度，杀毒软件误报少一些。

### 方式三：给源码（对方会 Python）

把 `代码/` 目录打包发过去，让对方：

```bash
pip install pygame
python game.py
```

适合对方本来就有 Python 环境的情况。

---

## 如果对方是 macOS

上面的 exe **不能**在 Mac 上运行。有两条路：

1. **让 Mac 用户跑源码**：装好 Python 和 pygame 后 `python game.py` 即可，
   代码本身是跨平台的（pygame 在 macOS 上正常工作）。
2. **在 Mac 上重新打包**：PyInstaller 不能跨平台打包，
   要在 Mac 上跑一次 `pyinstaller --onefile --windowed --add-data "resources:resources" game.py`
   （注意 Mac 上分隔符是冒号），会生成 `.app` 或可执行文件。

另外 macOS 的字体路径和 Windows 不同，需要把 `FONT_CANDIDATES` 换成
`/System/Library/Fonts/PingFang.ttc` 之类的路径。
